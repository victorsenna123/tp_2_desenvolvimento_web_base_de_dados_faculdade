@page
@model ViewDataModel

<h2>Dados Submetidos</h2>

@if (Model.FileContents.Count == 0)
{
    <p>O arquivo está vazio ou não foi encontrado.</p>
}
else
{
    <ul>
        @foreach (var line in Model.FileContents)
        {
            <li>@line</li>
        }
    </ul>
}
